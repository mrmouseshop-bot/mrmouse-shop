name: Обновить кэш миниатюр и полноразмерных фото

on:
  schedule:
    # каждый день в 03:00 по UTC (06:00 по Москве)
    - cron: '0 3 * * *'
  workflow_dispatch: {}   # позволяет запустить вручную кнопкой в вкладке Actions

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Установить зависимости
        run: pip install requests pillow

      - name: Собрать photos.js и папку photos/
        run: python build_photos.py
        env:
          NOCODB_TOKEN: ${{ secrets.NOCODB_TOKEN }}

      - name: Подтянуть свежие изменения (на случай правок во время сборки)
        run: git pull --rebase --autostash origin main

      - name: Закоммитить обновлённые файлы
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'Автообновление кэша миниатюр и полноразмерных фото'
          file_pattern: 'photos.js photos/ index.html'
